{
  "name": "shim_layer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prod": "export NODE_ENV=prod && nodemon index.js",
    "prod_base": "export NODE_ENV=prod && nodemon index.js --no_parallel",
    "prod_spawn": "export NODE_ENV=prod && nodemon index.js --dynamic_spawn",
    "prod_limit": "export NODE_ENV=prod && nodemon index.js --no_limit",
    "prod_dynamic_limit": "export NODE_ENV=prod && nodemon index.js --no_limit --dynamic_spawn"
  },
  "author": "Dpac99",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.4",
    "config": "^3.3.9",
    "dockerode": "^3.3.4",
    "express": "^4.18.1",
    "nodemon": "^2.0.20",
    "yargs": "^17.7.1"
  }
}